services:
  - type: web
    name: electricity-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python electricity_bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false